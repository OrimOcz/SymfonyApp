{% extends 'base.html.twig' %}

{% block body %}

<div class="container">
    <div class="row">
        <h3 class="mt-5 mb-5">form</h3>
        <div class="col-md-6 offset-md-3">
            {{ form_start(form) }}
                <div class="form-group">
                    {{ form_widget(form.title,{'attr':{'class':'form-control'}})}}
                </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>

{% endblock %}